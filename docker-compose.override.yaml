services:
  web:
    command: flask --app run:flask_app run --host=0.0.0.0 --debug
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    environment:
      FLASK_DEBUG: 1

  celery_worker:
    volumes:
      - .:/app